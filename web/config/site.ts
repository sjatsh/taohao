export type SiteConfig = typeof siteConfig;

export const siteConfig = {
    name: "TaoHao",
    description: "",
    pages: {
        orders: {
            search: "/orders/search",
            buy: "/orders/buy",
        }
    },
};
